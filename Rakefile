# This file could be a lot better if it took inspiration in
# https://github.com/rspec/rspec-rails/blob/master/Rakefile

require "bundler/gem_tasks"
require "cucumber/rake/task"

Cucumber::Rake::Task.new(:cucumber)

task :default => [:cucumber]
